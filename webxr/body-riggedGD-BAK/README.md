# Export the Godot "WebXR" preset into this folder as index.html (+ .wasm/.pck/etc).
# Then enable GitHub Pages: Settings → Pages → Deploy from branch → /docs

This folder is the GitHub Pages root after export.
